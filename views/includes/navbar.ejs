<script>
    window.addEventListener("scroll", function() {
        const navbar = document.querySelector(".navbar");
        if (window.scrollY > 50) {
            navbar.classList.add("scrolled");
        } else {
            navbar.classList.remove("scrolled");
        }
    });
</script>

<style>
    /* Navbar Background Fixed on Scroll */
    .navbar {
        transition: background 0.3s ease-in-out;
    }
    
    .navbar.scrolled {
        background: white !important;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    /* Custom Button Styles */
    .host {
        border-radius: 20px;
        background: transparent;
        border: none;
        padding: 10px 20px;
        transition: all 0.3s ease-in-out;
    }

    .host a {
        color: #007bff !important;
        text-decoration: none; /* Removed underline */
    }

    .host:hover {
        background: rgba(0, 123, 255, 0.1);
    }

    /* Navbar links */
    .nav-link {
        color: #555 !important;
        font-weight: 600;
        transition: color 0.3s;
    }

    .nav-link:hover {
        color: #007bff !important;
    }

    /* Removes underline for login/logout/signup */
    .auth-links a {
        color: #007bff !important;
        text-decoration: none;
        font-weight: 600;
        padding: 8px 15px;
        transition: background 0.3s ease-in-out;
        border-radius: 20px;
    }

    .auth-links a:hover {
        background: rgba(0, 123, 255, 0.1);
    }

    /* Responsive adjustments */
    @media (max-width: 992px) {
        .auth-links {
            text-align: center;
            margin-top: 10px;
        }

        .host {
            display: block;
            text-align: center;
            width: 100%;
        }
    }
</style>

<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top">
    <div class="container-fluid">
        <!-- Brand Logo -->
        <a class="navbar-brand d-flex align-items-center text-primary" href="/listing">
            <i class="fa-solid fa-car fa-xl me-2"></i>
            <span class="fw-bold">TravelMate</span>
        </a>

        <!-- Toggler Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/listing">Browse Vehicles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/about">About Us</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Contact Us</a>
                </li>
            </ul>

            <!-- Login/Signup Buttons -->
            <div class="ms-auto auth-links">
                <button class="host"><a href="/listing/new">Become Host</a></button>
                <% if (!currUser) { %>
                    <a href="/signUp">Signup</a>
                    <a href="/login">Login</a>
                <% } %>
                <% if (currUser) { %>
                    <a href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
